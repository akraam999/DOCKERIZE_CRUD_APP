version: "3"
   
services:
  db:
    image: postgres
    hostname: postgres
    volumes:
      - ./data/db:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=manageApp
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=admin
    ports:
      - 5432:5432
    networks:
      - mynetwork
  web:
    build:
      context: .
    ports:
      - "8000:8000"
    networks:
      - mynetwork
    volumes:
      - .:/prj:delegated
    command: python manage.py runserver 0.0.0.0:8000
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://postgres:admin@db:5432/manageApp

volumes:
  pgdb:
networks:
  mynetwork: